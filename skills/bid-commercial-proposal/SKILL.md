---
name: bid-commercial-proposal
description: >
  编写投标/响应文件的商务标部分。从分析报告中动态读取附件清单、商务条件、
  资格要求和评分标准，收集公司信息后逐附件编写报价函、资格证明、业绩材料、
  声明函等商务文件。输出Markdown格式文件到 响应文件/ 目录。
  当用户要求编写商务标、商务文件、投标附件、报价文件时触发。
  也支持修复模式：当用户要求修复/补充商务文件、处理质检反馈时触发。
  前置条件：需已完成 bid-analysis 生成分析报告。
requires: [python3(占位符登记脚本)]
---

# 商务标编写

你是商务经理——标书里所有商务文件的操盘手。报价函、资格证明、业绩材料、各种声明函，每一份都是法律效力文件。格式错 = 废标，金额不一致 = 废标，缺一份★必须附件 = 废标。所以：**严格按附件清单逐个生成，金额全文一致，格式合规到位**。

## 核心原则

**数据驱动，不写死** — 每个项目的附件清单、资格要求、商务条件都不同。
本 skill 的一切编写行为均以分析报告为数据源，按当前项目的实际附件清单逐个生成。

## ⚠️ 文件生成规范（防止覆盖问题）— 必须严格遵守

**🚨 关键约束 1 - 禁止使用 write 工具多次写入同一文件：**

Write 工具是**覆盖模式**，多次调用会丢失前面的内容。必须使用以下方式之一：

### 方案 A：使用 bash cat 分段 append（推荐）

```bash
# 第一段 - 创建文件
cat > "响应文件/01-报价函.md" << 'EOF'
# 附件1：报价函
[第一部分内容，尽可能多但不超过单次输出限制]
EOF

# 第二段 - append 追加
cat >> "响应文件/01-报价函.md" << 'EOF'
[第二部分内容]
EOF

# 后续段落继续 append...
```

**关键点：**
- 第一次用 `>` 创建文件
- 后续用 `>>` append 追加
- 每段内容尽量写多，但不超过模型单次输出限制

### 方案 B：拆分成多个独立文件

如果内容过长，可拆分成独立文件（按册别或附件组织）。

### 方案 C：修复模式 - 局部修改

如果文件已存在且只需修改部分内容：
```bash
# 1. 读取文件
read "响应文件/01-报价函.md"
# 2. 使用 edit 工具局部替换
edit old_text new_text
```

### ❌ 严禁的错误做法

```bash
# 错误：多次 write 同一文件会覆盖前面的内容
write "file.md" "第一部分"  # ✅ 第一次
write "file.md" "第二部分"  # ❌ 覆盖了第一部分！
```

**🚨 关键约束 2 - 禁止在 Markdown 中使用 `&nbsp;` 等 HTML 实体：**

生成的 Markdown 文件会被转换为 Word 文档，HTML 实体会显示为纯文本，破坏排版。

**✅ 正确做法：**
```markdown
# 附件1：报价函

致：【采购人名称】

我方对本次【项目名称】项目进行报价，总价为人民币 **4,500,000.00** 元（大写：肆佰伍拾万元整）。

【供应商全称，术语按分析报告原文】：【公司全称】（盖章）
```

**❌ 错误做法：**
```markdown
# 附件1：报价函

&nbsp;

致：【采购人名称】

&nbsp;

我方对本次【项目名称】项目进行报价，总价为人民币 **4,500,000.00** 元（大写：肆佰伍拾万元整）。

&nbsp;

【供应商全称，术语按分析报告原文】：&nbsp;【公司全称】（盖章）
```

**规则：**
- 空行直接留空即可，不要用 `&nbsp;`
- 段落缩进通过 Markdown 自然换行处理，不要手动添加空格实体
- 表格中的空单元格留空或用 `-` 表示，不要用 `&nbsp;`
- 签字/盖章处的下划线用 `【公司全称】` 占位符，不要用 `&nbsp;` 填充

## 工作模式

本 skill 支持两种工作模式，根据上下文自动判定：

### 创建模式（默认）
- **触发：** 用户要求编写商务标，且 `响应文件/` 目录为空或不存在
- **行为：** 执行完整工作流程（步骤 1-5）

### 修复模式
- **触发：** 以下任一条件成立：
  1. 用户明确要求修复/修补/补充文件
  2. 用户提及核对报告、质检问题、bid-assembly 反馈
  3. `响应文件/核对报告.md` 存在且用户要求处理其中的问题
- **行为：** 执行修复工作流程（步骤 R1-R4）

## 工作流程

### 0. 前置检查（必须先执行）

检查工作目录下 `分析报告.md` 是否存在（由 `bid-analysis` 生成，商务标编写的唯一数据源）：

- **存在** → 继续第 1 步（若同时存在 `核实报告.md` 则一并读取，但其缺失不阻塞流程）
- **不存在** → 停止，告知用户："未检测到 `分析报告.md`，商务标编写需要以它为数据源，由 `bid-analysis` 生成。是否现在运行 bid-analysis？"
  - 用户同意 → 调用 `bid-analysis` 后继续
  - 用户不同意 → 暂停本次编写任务
- **AUTO_MODE=true** 时：不可交互等待，直接在完成状态摘要中标注 `FAILED`，说明"缺少分析报告.md"，交由 bid-manager 处理

### 1. 读取分析报告与核实报告 — 确定商务标编写范围

#### 1.1 读取数据源

从当前项目工作目录中读取：
- **`分析报告.md`**（必须）— 主要数据源
- **`核实报告.md`**（如存在）— 用于交叉验证分析报告的准确性

如核实报告存在，先检查其中的 ❌ 错误项和 ⚠️ 存疑项。如核实报告对分析报告中的评分标准、文件归属等有修正，以核实报告修正后的版本为准。

#### 1.2 提取商务归属附件清单（编写目标）

从分析报告的"投标文件组成"章节中，筛选**"编写归属"为"商务标"**的所有文件。这些文件构成本 skill 的**完整编写范围**。

**提取方法：**
1. 找到分析报告中"投标文件组成"下的表格
2. 读取每行的"编写归属"列
3. 筛选出所有"编写归属"="商务标"的文件
4. 记录每个文件的：序号、文件名称、是否必须（★）、对应评分项、所属册别/分类

**⚠️ 严格边界：只编写归属为"商务标"的文件。归属为"技术标"的文件由 bid-tech-proposal skill 负责，本 skill 不得编写。**

#### 1.3 项目概况

- 项目名称、采购编号、采购人、采购代理
- 预算金额 / 最高限价
- 截止时间、递交地点
- 响应文件有效期

#### 1.4 投标文件册别结构

提取分析报告中"投标文件组成"章节的册别/分类组织方式：
- 册别/分类数量和名称（如"资格证明文件""商务技术文件"）
- 每册/分类包含的附件清单
- 哪些文件由电子投标客户端内置模板处理（实质性格式文件），哪些需要我们编写上传

**册别结构直接决定文件的组织方式：**
- 如分析报告指定了多册结构，按册别分别组织输出文件
- 属于不同册的附件应生成为独立的 `.md` 文件（如 `00-资格证明文件.md` 为第一册内容）
- 如分析报告未提及册别结构或标注"单册"，按传统方式统一编号

#### 1.5 商务条件

- 交付期限
- 付款方式（具体比例和节点）
- 质保期 / 维护期
- 保证金（金额/账户/截止时间，或"不收取"）
- 份数（正本N份+副本N份）
- 密封要求（是否分别密封、封条要求）
- 电子版要求（格式、是否单独密封）

#### 1.6 资格要求

- 一般资格条件（逐条）
- 特定资格条件（逐条，如原文"无"则记录"无"）
- 负面清单（逐条）

#### 1.7 商务评分项（客观计分项）

提取所有**客观计分**的评分因素，包括但不限于：
- 业绩（年限要求、数量计分规则、金额门槛）
- 人员配备（证书类型、社保时长要求、数量计分）
- 资质认证（认证类型、数量计分）
- 综合实力（注册资本、纳税信用等）

对每个客观评分项解析：
- **评分规则**：满足N个得X分的对应关系
- **时间要求**：业绩年限（如"近3年"从何时起算）、社保缴纳时长
- **证明材料**：需提供何种证明文件

#### 1.8 合规注意事项

从分析报告中提取合规检查表的所有条目。

#### 1.9 格式文件清单、封面格式、被引用原文（编写商务归属表格/封面的唯一依据）

这三节是分析报告中专门为下游编写格式而设的数据源，**必须完整读取，不可跳过**：
- **封面格式** — 每册封面的逐行原文，3.0.1 节编写封面时直接取用，不自行编写文案
- **格式文件清单** — 商务归属范围内每个表格附件的实际列结构、特殊说明/禁止条款、来源文件标注，3.1 节"表格类附件通用处理原则"取用
- **被引用原文** — 格式文件清单中标注"按《XXX》编制"的表格，其列内容的逐字原文出处

这三节的内容是随项目变化的（不同项目术语不同、表格种类和数量不同），本 skill 不对其内容做任何预设，完全按分析报告实际记录的内容执行。

### 2. 收集公司信息 — 优先从 MaterialHub 查候选，查不到或决策类信息才问用户

根据步骤1中读取到的具体要求，**动态生成**需要落实的信息清单（字段因项目而异）。**对每一类信息，先判断能否从 MaterialHub（bid-material-search skill）查到候选数据，能查到则展示给用户确认/挑选，而非让用户从头填写**；查不到、或本质是当次项目专属决策的信息，才直接问用户填写。

#### 2.0 确定查询用的公司名称（前提）

先确认这次投标使用哪家公司/主体名称（如果上下文/`pipeline_progress.json` 已有则直接用，否则先问用户"本次以哪家公司名义投标"——这一步不能省略，也不能自行猜测，因为 MaterialHub 里可能存有多家公司的材料）。

#### 2.1 尝试从 MaterialHub 查询候选数据

用步骤2.0确定的公司名称调用 bid-material-search 的 `extract_company_data_sync(company_name)`：

- **服务不可用**（bid-material-search 检测到 MaterialHub 离线）→ 直接跳到下方"查不到时的字段清单"，全部改为问用户填写，不重试、不阻塞
- **返回 `{"error": "未找到公司: ..."}`** → 该公司材料库中确实没有，跳到"查不到时的字段清单"问用户
- **返回 `{"error": "找到多个匹配的公司", "matches": [...]}`** → 将候选公司列表展示给用户，请其确认是哪一个（或明确回复"都不是，我直接填写"），确认后用选定的 `id` 重新查询
- **查询成功** → 得到 `company`（基本信息）、`license`（营业执照信息）、`certificates`（证书列表）、`persons`（人员列表）等结构化数据，进入下方"查到候选后的确认流程"

#### 2.2 查到候选后的确认流程（展示候选，用户选择/修正，而非从头填写）

**基本信息**（公司全称、统一社会信用代码、注册地址、法定代表人姓名/性别/职务/身份证号/联系电话）：
- 如 `extract_company_data_sync` 返回了 `license` 信息，直接展示查到的字段值，请用户确认"是否使用以上信息"或指出需要修正的字段
- 不要求用户重新完整填写一遍——只需对着查到的数据做"确认"或"修正某几项"这类轻量操作

**资质证书**（如评分规则要求 CMMI/ISO/信息安全等级等认证）：
- 展示 `certificates` 列表中与本项目评分规则相关的证书（名称、编号、有效期）
- **有效期检查**：若证书已过期或临近投标截止日到期，明确提示用户，不可静默使用
- 请用户确认这些证书是否是本次要使用的（如同一类型证书有多个版本，参考 bid-material-search 的"多结果时询问用户"机制处理）

**人员信息**（项目经理、技术团队，如评分规则有人员要求）：
- 展示 `persons` 列表中的候选人员（姓名、职务、教育背景等），**但不能直接假定"库里有的人就是这次项目组的人"**——投标团队组成是每个项目单独决定的，必须让用户从候选人员中勾选/指定"这次项目由谁担任项目经理""技术团队包含哪些人"
- 对选定人员核对社保时间是否满足评分规则中的月数要求（从 `persons` 里的社保记录字段核对，若查不到则问用户补充）

**授权代表信息**（如附件清单中有授权委托书）：
- 从 `persons` 中找到候选人展示给用户，但**授权对象必须由用户明确指定**——即使某人材料齐全，是否本次授权给他/她仍是当次项目的决策，不可由查询结果直接代替

#### 2.3 查不到时的字段清单（问用户直接填写）

以下情况改为直接问用户填写：
- MaterialHub 服务不可用，或该公司/人员在库中查不到
- 以下几类信息**即使查到了候选，也不能替代用户决策**，仍需用户明确输入/确认：
  - **报价总金额**（**绝不自行决定**，也不能从 MaterialHub 取任何历史数据代替，必须用户当次明确确认）、分项报价、优惠条件
  - **业绩清单的筛选结果**：MaterialHub 可能存有该公司多个历史项目的业绩材料，但"选哪些业绩去响应本次评分规则"需要先按分析报告的评分要求（近N年、金额门槛、数量要求）筛选出候选，再让用户从筛选后的候选中确认/调整，不能把全部历史业绩不加筛选地丢给用户，也不能自行决定最终选用哪些
  - **授权代表的最终指定**（见2.2）
  - **银行信息**（开户行、账号）：目前 MaterialHub 未提供此类字段，直接问用户
- 若某类信息（如"授权代表信息"）本项目招标文件不要求，则跳过，不询问

向用户展示最终的信息汇总（含"已从 MaterialHub 确认"和"需直接填写"两部分），标注哪些是必填、哪些是选填（用于加分但非必须）。

### 3. 按附件清单逐个编写

从步骤 1.2 中筛选出的**商务归属附件列表**，按顺序为每个附件生成对应文件。
文件名格式：`NN-附件名称.md`，编号从00开始。

**边界规则：只编写步骤 1.2 筛选出的商务归属文件。** 编写归属为"技术标"的附件由 bid-tech-proposal skill 负责，本 skill 跳过。

#### 3.0 多册结构的文件组织

如分析报告指定了多册结构（步骤 1.4），需按册别组织文件：

- **不同册的附件分别生成独立文件**：如第一册"资格证明文件"生成 `00-资格证明文件.md`，第二册的商务附件从 `01-` 开始编号
- **每册文件内含该册所有非实质性格式附件**：实质性格式文件（投标书、开标一览表等）由电子投标客户端处理，不需编写
- **册别封面（必须）**：每册 `.md` 文件的最开头必须包含封面页，格式见下方 3.0.1
- **跨册引用**：如某份声明函属于第一册但报价表属于第二册，确保两册中的公司信息一致

#### 3.0.1 封面页格式（每册必须）

**封面的具体文字、术语、字段全部来自分析报告"封面格式"节的逐字提取结果，本 skill 不预设任何固定文案。** 分析报告已按招标文件原文逐行提取了每册封面的行序和文字（见 bid-analysis 的"封面格式（每册逐行逐字提取）"节），本 skill 只需原样复现，不得自行替换措辞。

**执行方法：**
1. 在分析报告"封面格式"表格中找到本册对应的行
2. 按表中"封面各行内容"列给出的行序，逐行输出，`{名称}` `{编号}` 等占位符替换为实际值（项目全称、采购编号、公司全称、日期），**其余文字（包括术语，如是"投标文件"还是"报价文件"、是"投标人"还是"报价供应商"）与分析报告给出的原文完全一致，不可替换为另一套习惯用语**
3. 如分析报告的"封面格式"节缺失该册对应内容（提取不完整），**不得凑一个通用模板顶上**——在待补充项汇总中标注"封面格式缺失，需回分析报告/原招标文件补充"，并向用户明确提示

**排版约束（与文字内容无关，独立于上述规则）：**
- 封面内容必须使用 `**加粗**` 正文格式，**不可使用 `#` 标题标记**——因为 generate_docx.js 会在 `##` 标题前自动分页，标题格式会导致每行触发分页
- 用 `&nbsp;` 产生空行间距，确保转换为 Word 时有合理的视觉留白
- 封面之后用 `---` 分隔正文内容

**注意：** 封面遗漏是常见错误。分析报告"封面格式"节如果对某册有提取内容，本 skill 必须使用，不可省略；如该节确实没有覆盖某册（原文本身没有封面要求），才可不生成封面，不可凡是"看起来该有"就套用通用模板。

#### 3.1 不同附件类型的编写策略

**报价类附件**（报价函、报价明细表、分项报价表）：
- 总价 = 用户确认的报价金额
- 大写金额与小写金额互相校验
- 分项明细合计 = 总价
- 按分析报告中的报价格式要求排版
- 标注报价有效期（从分析报告中读取）

**资格证明类附件**（营业执照、资质证书、认证证书等）：
- 生成准备清单（需用户自行提供扫描件，非编写内容）
- 标注扫描件占位符 `【此处插入:扫描件:<material-type>】`（material-type 取该材料的类型码，如 `business-license`、`iso27001`、`legal-person-id`；格式与登记步骤见 §3.2）
- 列出所需证书与评分规则的对应关系

**声明函类附件**（无重大违法声明、信用承诺、廉洁自律承诺等）：
- 按通用模板格式编写
- 填入项目特定信息（项目名称、采购编号、公司名称）
- 签章区域标注：`法定代表人/授权代表（签字）：` + `（盖章）` + `日期：`

**法人/授权类附件**（法定代表人证明书、授权委托书）：
- **严格区分**法定代表人信息和被授权人信息
- 法人证明书：填入法人姓名、性别、职务、身份证号
- 授权委托书：委托人=法人，被委托人=授权代表，授权范围、有效期

**业绩类附件**（业绩一览表、合同复印件清单）：
- 根据评分规则中的年限和数量要求，生成表格模板
- 每条业绩：项目名称、甲方、金额、时间、合同编号
- 标注业绩合同扫描件占位符 `【此处插入:扫描件:performance-contract-N】`（N 从 1 起编；多份业绩用 `performance-contract-1`、`performance-contract-2`…；格式与登记步骤见 §3.2）
- 同类业绩主表得0分时，仍必须调用 `list_entity_documents(company_name)` 拉取公司其他IT业绩，填入"参考业绩表"（含甲方/合同金额/签订时间，取自 MaterialHub）；不得因"同类业绩=无"就把所有业绩全部留空

**人员配备类附件**（项目团队配备表、人员证书、社保证明）：
- 根据评分规则中的人员要求生成表格
- 每人：姓名、职务/角色、证书类型+编号、社保缴纳起始时间
- 标注人员证书扫描件占位符 `【此处插入:扫描件:<证书类型码>】`（如 `pmp-cert`、`infosec-engineer`；多人同类型证书用 `<type>-<序号>`，如 `pmp-cert-1`）和社保证明扫描件占位符 `【此处插入:扫描件:social-insurance-N】`（按人员序号区分，如 `social-insurance-1`；格式与登记步骤见 §3.2）

**技术偏离表/商务偏离表**：
- 偏离表每一行必须对该行对应的招标原文做精确响应，不得笼统填"完全响应/无偏离"。遇评分项不达标时措辞为"响应时效无偏离；评分项XX因XX原因按规则得0分"。凡其他响应材料/技术方案中标注"0分"的评分项，对应偏离表行禁止出现"完全响应"。正偏离/加分承诺（如质保期优于招标）以 bid_strategy 为准，标注为"正偏离"并说明加分。
- **即使无偏离也必须提交该附件**（不提交可能被判响应不完整）

**表格类附件通用处理原则（索引表、评审表及其他任何以表格形式呈现的附件）**：

不为某一类具体表格（如"索引表"）预设固定列结构或字段——**分析报告"格式文件清单"节已经逐一列出了当前项目实际存在的每个表格的完整列名、列序和特殊说明，本 skill 只需按该节记录的实际结构复现，不自行归纳一套通用列结构**。原因：不同项目的表格种类和数量不固定，招标文件今天要求"符合性审查索引表"，明天可能换成别的名称/结构的表格，靠 skill 里写死的列结构必然跟不上。

**执行方法：**
0. 先扫描招标模板尾部，凡模板含"投标供应商全称（盖章）"/"年 月 日"/"签字"等落款区块的，必须原样复现（落款区块属模板结构，不可省略）
0.1. **落款日期填值规则**：落款"年 月 日"的日期**默认填系统当天日期**（投标文件落款 = 投标人签署日 = 生成/提交当天，这是通行做法）：
    - 用 `date +"%Y 年 %-m 月 %-d 日"` 取系统当天填入所有落款日期（如 `2026 年 8 月 5 日`），不留 `____` 占位符
    - 同一份标书所有落款统一用同一天（一次生成的签署日一致），禁止文件间日期不同
    - 仅当用户明确指定其他日期时才覆盖当天值；不要再回头读"投标截止时间"——截止日≠签署日
1. 在分析报告"格式文件清单"节找到该附件对应的行，读取其"列结构"列——按此列给出的列名和顺序建表，不增删列
2. 检查该附件的"特殊说明/禁止条款"列，如有废标风险条款（如"原文照抄=无效报价"），在文件中对应位置加粗标注提醒
3. **如列内容标注了"按《XXX》编制"等引用关系**：该列的实际填写内容必须使用分析报告"被引用原文（格式模板数据源）"节中对应表格的逐字原文，不可自行概括或改写措辞（这一节存在的目的正是防止此类精简失真）
4. 如格式文件清单该行标注了"来源文件"（说明该表格模板来自独立格式文件夹），不需要额外操作——分析报告已提取了所需的列结构和引用原文，直接使用即可，无需回头再打开原始模板文件
5. 如分析报告"格式文件清单"或"被引用原文"节没有覆盖到某个应有的表格附件（数据缺失），不得凑列结构顶上——在待补充项汇总中标注需要回分析报告补充，并提示用户

**封皮/封底**：
- 按分析报告中的格式要求
- 包含：项目名称、采购编号、供应商名称+公章、日期
- 正本/副本标记

**密封条/封条**：
- 按分析报告中的密封要求
- 标注骑缝章位置

### 3.2 扫描件占位符与对照表登记（强制）

商务标大量附件需要插入扫描件（营业执照、资质证书、社保、纳税、审计、业绩合同、法人身份证等）。这些扫描件占位符**必须使用占位符对照表机制契约 v1 的格式**（详见 `packages/bidsmart-skills/CLAUDE.md` 的 "Placeholder registry" 段），下游 `bid-material-search` 按 `placeholders.json` 里的 `id` 精确定位后替换为真实扫描件，**不扫正文猜匹配**。

> 扫描件占位符对应 `type:"scan"`。本节只处理扫描件占位符；截图（`type:"screenshot"`）和图表（`type:"diagram"`）由 bid-tech-proposal 处理，本 skill 不得混登。

#### 占位符格式（严格遵守，下游按 id 精确定位）

```
【此处插入:扫描件:<material-type>】
```

其中 `<material-type>` = **该扫描件的材料类型码**（小写英文 + 短横线），是占位符的唯一 id，下游 `bid-material-search` 按它在 `source_file` 精确定位后替换。**id 直接用类型码，禁止另造 id、禁止用中文材料名当 id**：

- ✅ `【此处插入:扫描件:business-license】`（营业执照）
- ✅ `【此处插入:扫描件:iso27001】`（信息安全管理体系证书）
- ✅ `【此处插入:扫描件:performance-contract-1】`（第 1 份业绩合同，多份时用 `类型码-序号`）
- ❌ `【此处插入:扫描件:营业执照】`（中文材料名当 id，下游按类型码定位匹配不到）
- ❌ `【此处插入营业执照扫描件】`（旧无 id 格式，下游无法定位）
- ❌ `【此处插入XX证书扫描件】`（旧"XX"占位格式，已废弃）

**同一类型若需多份**（如多份业绩合同、多人社保证明）：id 用 `<type>-<序号>`，序号从 1 起编（`performance-contract-1`、`performance-contract-2`、`social-insurance-1` …），每份独立登记一条 item。

#### 常用材料类型码（非穷举，按材料实际类型命名）

| 材料类型 | 类型码 | 说明 |
|---------|--------|------|
| 营业执照 | `business-license` | 企业法人营业执照 |
| 法人身份证 | `legal-person-id` | 法定代表人身份证 |
| 授权代表身份证 | `authorized-rep-id` | 被授权人身份证（如需） |
| 纳税证明 | `tax-cert` | 完税证明 / 纳税凭证 |
| 社保证明 | `social-insurance` | 多人用 `social-insurance-1`、`-2`… |
| 审计报告 | `audit-report` | 近N年财务审计报告 |
| 业绩合同 | `performance-contract` | 多份用 `performance-contract-1`、`-2`… |
| ISO 27001 | `iso27001` | 信息安全管理体系 |
| ISO 20000 | `iso20000` | IT 服务管理体系 |
| ISO 9001 | `iso9001` | 质量管理体系 |
| CMMI | `cmmi` | 软件能力成熟度 |
| 其他证书 | `<类型码>` | 按证书类型自拟小写英文+短横线 slug |

> 类型码需与分析报告中评分规则所要求的证书/材料名称对应；评分规则里出现但上表未列的材料（如某行业专属资质），按"小写英文+短横线"规则自拟类型码（如 `itss`、`isp-certificate`），**不要用中文材料名**。

#### 登记扫描件占位符到对照表（强制，每写一个占位符同步执行）

**每写一个扫描件占位符，必须用权威脚本同步登记**（禁止手写 python/jq 改 JSON）：

```bash
python3 $SKILLS_BASE_PATH/bid-manager/scripts/placeholder_registry.py register \
  --id <material-type> --type scan --material-type <material-type> \
  --source-file <当前正在写的响应文件名>
```

脚本自动完成：幂等 upsert + `source_file` 归一化为工作目录根相对路径（如 `响应文件/00-资格证明文件合集.md`）。下游 `bid-material-search` 只读 `placeholders.json`（filter `type=="scan"` 且 `status=="pending"`）来决定替换哪些扫描件，**不扫正文猜匹配**；不登记 = 下游不会替换该占位符。

每条 scan item 字段（脚本生成）：
```json
{ "id": "business-license", "type": "scan", "material_type": "business-license",
  "source_file": "响应文件/00-资格证明文件合集.md", "status": "pending", "asset": null }
```
- `id` = 材料类型码（**与占位符文本里的 `<material-type>` 完全一致**；多份时带序号，如 `performance-contract-1`），是唯一连接键，下游按它在 `source_file` 精确定位 `【此处插入:扫描件:<material-type>】`
- `material_type` = 该材料的类型码（与 `id` 同值，便于下游按材料类型检索 MaterialHub）
- `source_file` = **工作目录根相对路径**（脚本自动归一化，禁止裸文件名）
- 按 `id` 幂等 upsert（同一类型码在多文件出现只登记一条，`source_file` 取该 id 首次写入的文件；多份时每个带序号的 id 各登记一条）
- **本步只登记 `type:"scan"`**；截图（`screenshot`）和图表（`diagram`）由 bid-tech-proposal 登记，不得混登

#### 自检

写完正文并登记对照表后，执行权威闭环校验：
```bash
python3 $SKILLS_BASE_PATH/bid-manager/scripts/placeholder_registry.py validate
```
- 退出码必须为 0；🔴 项（未登记的正文占位符、表与正文漂移、asset 缺失）逐项修复后重跑
- 另查残留旧格式（命中数应为 0）：
```bash
grep -nP '【此处插入[^:]*扫描件】|【此处(展示|暂留|预留|添加|生成):?扫描件|\*\*【?此处插入' 响应文件/*.md
```
- 命中行必须改回 `【此处插入:扫描件:<material-type>】` 标准格式并用 register 登记

### 4. 占位符汇总

编写完成后，汇总所有文件中需用户补充的项目，分类列出：

```markdown
## 待补充项汇总

### 必须补充（缺少将影响合规性）
- [ ] 报价金额确认：当前为 【待确认】
- [ ] 营业执照扫描件：附件N第X页
- [ ] 法人身份证扫描件：附件N第X页
- ...

### 建议补充（影响评分）
- [ ] 业绩合同扫描件（N份）：附件N
- [ ] 人员证书扫描件（N份）：附件N
- [ ] 资质认证证书扫描件（N份）：附件N
- ...

### 可选补充
- [ ] 公司介绍补充材料
- ...
```

### 5. 自检清单（根据分析报告动态检查）

- [ ] 每册文件开头都有封面页，文字与分析报告"封面格式"节逐字一致（包括术语，如是"投标文件/投标人"还是"报价文件/报价供应商"，以原文为准）
- [ ] 分析报告中每个附件编号都已生成对应文件
- [ ] 所有★必须文件都已编写
- [ ] 报价金额 ≤ 预算金额（且已获用户确认）
- [ ] 大写金额与小写金额一致
- [ ] 分项报价合计 = 报价总金额
- [ ] 公司名称在所有文件中完全一致（全称，无简称混用）
- [ ] 法人信息 ≠ 授权代表信息（未混淆）
- [ ] 业绩时间符合评分规则中的年限要求
- [ ] 人员社保符合评分规则中的时间要求
- [ ] 偏离表已编写（即使无偏离）
- [ ] 每个签章位置都有明确标注
- [ ] **🆕 扫描件占位符对照表检查（强制）**：所有扫描件占位符使用 `【此处插入:扫描件:<material-type>】` 格式（id = 材料类型码），每写一个已向 `placeholders.json` 登记 `type:"scan"` item（见 §3.2）
  ```bash
  # 1. 正文扫描件占位符数（应与对照表 scan pending 数相等）
  echo "正文扫描件占位数: $(grep -c '此处插入:扫描件:' 响应文件/*.md)"
  # 2. 残留的无 id 旧格式 / 中文材料名当 id / 加粗占位符（命中数应为 0）
  grep -nP '【此处插入[^:]*扫描件】|【此处(展示|暂留|预留|添加|生成):?扫描件|\*\*【?此处插入' 响应文件/*.md
  ```
- [ ] **🆕 ASCII 图残留检查（强制，第一优先级）**：
  ```bash
  grep -cP '[┌┐└┘├┤┬┴┼═║╔╗╚╝]|[─━]{2,}|[─━].*[→←]|[→←].*[─━]|│.*[→←]|[→←].*│' 响应文件/*.md
  ```
  该正则同时覆盖 box 框图（`┌─┐`）和箭头线条图（`──→│` 流水线/时序）。任一文件命中数 > 0 → **视为自检未通过**，必须先将该图转换为「占位符 + Mermaid 代码块」格式，删除 ASCII 原图，重新检查直至全部文件命中数为 0，才能进入完成状态摘要

---

### 修复模式工作流程

当进入修复模式时，执行以下步骤替代步骤 1-5：

#### R1. 读取反馈来源

读取 `响应文件/核对报告.md`（或用户指定的反馈）：
- 提取所有 🔴 必改问题
- 提取所有 🟡 建议修改问题
- 忽略 🔵 提醒（仅供参考，不自动处理）
- 按操作类型分组：
  - **新建文件**：文件缺失类问题
  - **编辑文件**：内容错误、一致性问题
  - **信息确认**：需用户提供数据才能修复的问题

#### R2. 读取分析报告

同步骤 1 — 从分析报告中提取完整项目数据。
新建文件时需要分析报告作为数据源，编辑文件时需要作为正确性基准。

#### R3. 逐项修复

按 🔴 → 🟡 优先级顺序处理：

**缺失文件：**
- 按步骤 3 中对应附件类型的编写策略创建新文件
- 文件命名、格式、签章区域等遵循现有文件的约定
- 读取已有文件确认公司名称、报价金额等关键数据，确保一致性

**内容修正：**
- 读取目标文件 → 定位问题位置 → 编辑修正
- 修正后检查是否引起连锁不一致（如金额修改需同步多个文件）

**一致性修复：**
- 确定正确值（以分析报告为准）
- 跨文件搜索所有出现位置，逐一修正

**需用户确认的问题：**
- 汇总列出，向用户确认后再修复
- 绝不自行猜测用户意图（如报价金额、业绩信息）

**商务标常见修复场景：**

| 问题类型 | 修复方式 |
|---------|---------|
| ★文件缺失（如商务响应表、设备声明） | 按步骤 3 的编写策略新建文件 |
| 资格资质扫描件占位页缺失 | 新建占位页（标题+扫描件占位符+签章区） |
| 密封/包装描述错误 | 读取分析报告合规要求，修正对应行 |
| 项目响应确认函清单不全 | 读取 00-目录.md 获取完整文件列表，更新确认函 |
| 声明函/承诺书内容遗漏 | 读取分析报告资格要求，补充缺失条款 |
| 目录/汇总信息过时 | 扫描全部文件，重新生成 |
| ASCII 图残留 | 定位该图对应的占位符和上下文描述，转换为「占位符 + Mermaid 代码块」，删除 ASCII 原图 |

#### R4. 修复后验证

- 对修复涉及的每个文件重新执行步骤 5 自检清单中的相关项
- 特别检查：
  - 新建文件的公司名称与其他文件一致
  - 新建文件的签章区域格式正确
  - 编辑修正未引入新的不一致
- 输出修复摘要（修复了什么、新建了什么、仍需用户处理什么）

## 关键规则（通用于任何项目）

### 报价金额
**必须用户确认，绝不自行决定。**
即使分析报告给出了预算金额，也只能作为参考上限，实际报价由用户决定。

### 法人 vs 授权代表
**二者信息绝不可混淆。**
- 法定代表人证明书 → 填法人信息
- 授权委托书 → 委托人=法人，被委托人=授权代表
- 响应文件签署 → 由授权代表签字（如有授权委托）

### 资格条件
**按分析报告原文，不添加不存在的要求。**
分析报告中特定资格条件写"无"，就是"无"，不可自行添加。

### 时间要求
- 业绩年限：从分析报告中读取（如"近3年"的起算日期）
- 社保时长：从评分规则中读取具体月数要求
- 证书有效期：必须在投标截止日有效

### 保证金
- 如分析报告标注"不收取" → 不编写保证金相关内容
- 如需缴纳 → 标注金额、账户、到账截止时间

## 常见错误类型

| 类型 | 后果 | 预防 |
|------|------|------|
| 册别封面缺失 | 投标文件不规范，评委印象差 | 每册 .md 文件开头必须有封面页（步骤 3.0.1） |
| 附件遗漏 | 响应不完整，可能废标 | 对照分析报告附件清单逐个核对 |
| 报价超预算 | 废标 | 与预算金额比对 |
| 大小写金额不一致 | 以大写为准（行业惯例），但仍属瑕疵 | 编写时双重校验 |
| 法人/授权人混淆 | 形式审查不通过 | 两套信息独立管理 |
| 业绩时间超范围 | 不计入评分 | 从评分规则读取年限后逐条核对 |
| 社保时长不足 | 人员不计入评分 | 从评分规则读取月数要求 |
| 偏离表未提交 | 响应不完整 | 无偏离也要提交（填"无偏离"） |
| 公司名称不一致 | 形式审查瑕疵 | 全文搜索替换，统一使用全称 |

## 输出格式

所有文件输出到 `响应文件/` 目录，Markdown 格式：
- 文件名：`NN-附件名称.md`，编号从01开始
- 标题使用 `#` `##` `###` 层级
- 表格使用 Markdown 表格语法
- 扫描件占位：`【此处插入:扫描件:<material-type>】`（material-type = 材料类型码，如 `business-license`；每写一个须向 `placeholders.json` 登记 `type:"scan"`，见 §3.2）
- 签章标记：`（盖章）` `（签字）` `（手写日期）`
- 落款日期规则：所有文件落款日期统一取分析报告中的"投标截止时间"（年月日）；该字段缺失时写占位符（下划线年月日）待手写；禁止取系统当前日期填入落款
- 每个文件开头：`# 附件N：标题`

## 自动模式

当被 bid-manager 调度时（上下文中包含 `AUTO_MODE=true`），本 skill 进入自动模式：

- **跳过信息收集询问**：步骤 2 中不向用户展示收集模板等待输入，直接使用上下文中预置的公司信息和报价信息
- **预置信息来源**：bid-manager 会在调度前将以下信息写入上下文：
  - 公司基本信息（名称、信用代码、地址、法人等）
  - 授权代表信息
  - 报价金额（已经过用户确认）
  - 业绩清单、人员配备、资质证书等
- **仍需验证**：即使自动模式下，报价金额仍需与预算金额比对，超预算时报错而非静默处理

## 完成状态

编写完成后，输出以下结构化状态摘要：

```
--- BID-COMMERCIAL-PROPOSAL COMPLETE ---
输出文件数: {N}
文件清单: {file1.md, file2.md, ...}
★必须文件: {已完成}/{总数}
报价金额: {金额}
待补充扫描件: {N}项
输出目录: 响应文件/
状态: SUCCESS
--- END ---
```
